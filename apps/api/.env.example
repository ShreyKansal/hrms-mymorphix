# Copy this to .env and fill in real values. Never commit the actual .env file.
# See ../../README.md "Database" section for how the local dev database/role is set up.

DATABASE_URL="postgresql://hrms_app:CHANGE_ME@localhost:5432/hrms_dev?host=/tmp"
PORT=3000
